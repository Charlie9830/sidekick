import 'package:flutter/material.dart';

class NoItemFallback extends StatelessWidget {
  const NoItemFallback({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
